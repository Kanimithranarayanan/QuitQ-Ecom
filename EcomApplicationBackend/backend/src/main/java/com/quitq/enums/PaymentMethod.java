package com.quitq.enums;

/**
 * Supported payment methods for placing an order.
 * Currently only Cash on Delivery is supported; more methods
 * (e.g. ONLINE_PAYMENT) can be added later without breaking existing data.
 */
public enum PaymentMethod {
    CASH_ON_DELIVERY
}
